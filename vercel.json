{
  "framework": "vite",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm exec vite build",
  "devCommand": "pnpm exec vite --host 0.0.0.0 --port 3000",
  "outputDirectory": "dist"
}
